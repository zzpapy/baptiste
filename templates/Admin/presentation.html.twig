{% extends 'base.html.twig' %}

{% block body %}
<section id="presentation" class="home-container">
    <div class="card card-presentation">
        <h2>Présentation</h2>  
        <!-- <div class="flexPres">
            <div class="card "> -->
            <img id="img-pres" src="{{presentation.photo}}" alt="">  
                {{ form_start(presForm) }}
                        {{ form_row(presForm.photo) }}
                        
                        {{ form_row(presForm.text) }}
                        <button type="submit" class="btn btn-warning">Modifier</button>
                    {{ form_end(presForm) }}      
            <!-- </div>
            </div> -->
        </div>
    </div>
</section>
{% endblock %}
