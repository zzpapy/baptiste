{% extends 'base.html.twig' %}

{% block body %}
<section id="honoraire" class="home-container">
    <div class="card card-home">
        <h2>Honoraires</h2> 
        {{form_start(formHon)}}
        {{ form_start(formHon) }}
        {{ form_row(formHon.text) }}
        <button type="submit" class="btn btn-warning">Modifier</button>
    {{ form_end(formHon) }}        
    </div>
</section>
{% endblock %}
